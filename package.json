{
  "name": "calculator-app",
  "version": "1.0.0",
  "private": true,
  "description": "Browser-based calculator application",
  "scripts": {
    "test": "jest --verbose"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0"
  }
}
